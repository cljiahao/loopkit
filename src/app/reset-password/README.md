# reset-password

## Purpose

Password-reset landing page route entry, reached after the Supabase
recovery-session callback — the actual UI lives in `@/features/auth`.

## Contents

- `page.tsx` — `ResetPasswordPage`: thin route entry rendering
  `ResetPasswordForm` (from `@/features/auth`)

## Parent

[app](../README.md)
