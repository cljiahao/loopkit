NEXT_PUBLIC_BASE_URL=http://localhost:3000
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=
MERQO_METRICS_SECRET=
# Optional: per-IP rate limiting on the public /c surface. Leave blank to
# disable (the limiter fails open). Provision an Upstash Redis (free tier) and
# paste its REST URL + token to enable.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
